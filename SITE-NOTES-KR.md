# Project Sales Skill | 소개 페이지 안내

저장소 루트의 `index.html` 하나가 전체 웹페이지이다. CSS, JavaScript, EN/KR 문구, 작업 예시, 두 언어의 LITE 원문을 파일 안에 포함했다. 빌드, 패키지 설치, 외부 폰트, CDN, 분석 스크립트, 백엔드가 필요하지 않다.

## 열기와 배포

파일을 직접 열거나 저장소 루트에서 `python -m http.server 8000`을 실행한다. GitHub Pages에 배포할 때는 `index.html`이 있는 저장소 루트가 제공되도록 설정한다. 정적 파일을 그대로 제공하도록 `.nojekyll`을 포함했다. 이 패키지는 저장소 설정을 변경하거나 공개 배포를 수행하지 않는다. 저장소 CTA는 `https://github.com/JTech-CO/Project-Sales-Skill/`로 연결된다.

`index.html`만 다른 폴더에 옮겨도 내부 기능은 동작한다. 저장소의 문서 링크는 인터넷 연결이 필요하지만 LITE 복사와 다운로드는 로컬에서 동작한다. 페이지는 패키지의 다른 파일을 가져오지 않는다.

## 사용 기능

첫 방문 기본 언어는 EN이다. EN/KR 버튼은 본문과 관련 메타데이터를 전환하며 `?lang=en`, `?lang=ko`는 저장된 설정보다 우선한다. 언어 설정만 저장하며 저장소 접근이 차단되어도 전환할 수 있다. 작업 선택 예시는 실제 모델 실행이 아닌 설명용 샘플이다. 웹/앱/CLI 탭은 실제 패키지의 사용 경로를 안내한다.

LITE 복사는 클립보드를 우선 사용하고, 사용할 수 없으면 텍스트 선택 기반 복사를 시도한다. 복사까지 차단되면 원문을 선택하고 수동 복사가 필요하다고 표시한다. 다운로드는 현재 언어의 LITE 원문과 같은 내용의 `.md` 파일을 만든다.

시스템 폰트와 한국어 대체 폰트, 키보드 포커스, 반응형 재배치, 동작 줄이기 설정을 적용했다. JavaScript를 끈 환경에서는 영문 설명·LITE 원문·저장소 링크를 유지하고 동작하지 않는 인터랙션 버튼은 숨긴다.

## 유지보수

LITE 문서를 수정하면 `index.html`에 포함된 JSON과 초기 textarea도 동기화한다. 페이지 소스 테스트가 원문 차이를 검사한다. 릴리스에는 `package-info.json`, 페이지 버전, 스키마, SKILL 메타데이터를 함께 갱신하고 `dist/`와 `CHECKSUMS.sha256`를 다시 만든다. 실제 검증 범위는 [VALIDATION.md](VALIDATION.md)에 기록한다.
